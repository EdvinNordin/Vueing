<script>
export default {
  data() {
    return {
      projects: [
        {
          name: "Master Thesis",
          description: "Water",
          isHovering: false,
        },
        {
          name: "WebFighting",
          description: "Game",
          isHovering: false,
        },
        {
          name: "TabsShare",
          description: "Tabs",
          isHovering: false,
        },
        {
          name: "PokeChart",
          description: "Visualization",
          isHovering: false,
        },
        {
          name: "Corporate Combat",
          description: "AR Game",
          isHovering: false,
        },
        {
          name: "Color me Home",
          description: "Game",
          isHovering: false,
        },
        {
          name: "Cloudy",
          description: "Cloud Representation",
          isHovering: false,
        },
        {
          name: "Monte-Carlo Raytracing",
          description: "Custom Raytracing Renderer",
          isHovering: false,
        },
        {
          name: "Image Reproduction",
          description: "Images made out of images",
          isHovering: false,
        },
      ],
    };
  },

  methods: {
    hovering(project) {
      project.isHovering = true;
    },
    stopHovering(project) {
      project.isHovering = false;
    }
  }
};
</script>

<template>
  <section class="hero">
    <div class="hero-body container">
      <h1 class="title is-1">Edvin Nordin</h1>
    </div>
  </section>

  <section class="section">
    <div class="level horizontal-scroll is-nowrap">
      <div v-for="project in projects" :key="project.name">

        <a :href="project.link">
          
        <div class="level-item card is-flex-direction-column" 
        @mouseover="hovering(project)" 
        @mouseleave="stopHovering(project)">

          <h2 class="title card-content" 
          :class="project.isHovering ? 'is-1' : 'is-5'">
          {{ project.name }}</h2>

          <h3 class="subtitle card-content">
            {{ project.description }}</h3>
        </div>

        </a>
      </div>
    </div>
  </section>
  <section class="section">
    
    </section>
</template>

<style>
.horizontal-scroll {
  overflow-x: auto;
  overflow-y: hidden;
  white-space: nowrap;
}

/*
.small-size {
  width: 250px;    
  height: 250px;
}

.big-size {
  width: 300px;      
  height: 250px;     
}*/
</style>

<!--<div v-if="project.name === 'Master Thesis'" class="level-left card">
            <h3 class="title is-nowrap card-title">{{ project.name }}</h3>
            <p class="subtitle card-content">{{ project.description }}</p>
          </div>
          <div v-else-if="project.name === 'Image Reproduction'" class="level-right card" >
            <h3 class="title is-nowrap card-title">{{ project.name }}</h3>
            <p class="subtitle card-content">{{ project.description }}</p>
          </div>-->
